# Environment Configuration Example for NestJS Backend

# Database Credentials
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=stockquest


NODE_ENV=development
PORT=3000

# Database Connection (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:placeholder_password@localhost:5432/stockquest

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Authentication (JWT)
# Use a long, random string for the secret in production
JWT_SECRET=provide_a_secure_random_secret_here
JWT_EXPIRES_IN=7d

# Internal Service Connections
# The Python/FastAPI service for specialized stock data logic
AI_SERVICE_URL=http://localhost:8000